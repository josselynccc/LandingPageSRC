{
  "name": "landing_page_colegio",
  "version": "1.0.0",
  "description": "LandingPage para un colegio",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "scss": "node-sass --watch scss -o css",
    "build": "node-sass scss -o css && mkdir -p dist && cp index.js dist && cp index.html dist && cp css/styles.css dist"
  },
  "author": "Jcortez",
  "license": "ISC",
  "dependencies": {
    "swiper": "^11.0.6"
  },
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
